<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>iMeets</title>
    <!-- favicon -->
    <link rel="icon" type="image/x-icon" href="../images/logo.png" />
    <!-- Bootstrap cdn links -->
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
    crossorigin="anonymous"
  />
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
    crossorigin="anonymous"
  ></script>
    <!-- stylesheet -->
    <link rel="stylesheet" href="../styles/style.css" />
    <link rel="stylesheet" href="../styles/connectionsStyle.css" />
    <!-- scripts -->
    <!-- <script src="./script.js" defer></script> -->
  </head>

  <body>
    <!-- header section -->
   <%- include("../partials/nav.ejs") %>
    <!-- main content -->
    <main class="container content-container">
      <div class="jumbotron" id="listOfCategoriesContainer">
        <% if(connections.length) { %>
        <h1>Categories in I-meet</h1>
        <% for (const categoryObj of categories) { %>
          <h2><%= categoryObj %></h2>
          <ul>
              <% for (const connection of connections) { %>
                  <% if(connection.category === categoryObj) { %>
                  <li><a href="/connections/<%= connection.id %>"><%= connection.title %></a></li>
              <% } } %>
          </ul>
      <% } %>
        <%  } else { %>
        <h4>There are no categories in I-meet. Add new connection from <a href="/connections/newConnection">here</a></h4>
        <% } %>
      </div>
    </main>
    <!-- footer section -->
    <%- include("../partials/footer.ejs") %>
  </body>
</html>
