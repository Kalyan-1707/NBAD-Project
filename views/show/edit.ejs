<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>iMeets</title>
    <base href="/">
    <!-- favicon -->
    <link rel="icon" type="image/x-icon" href="../images/logo.png" />
    <!-- Bootstrap cdn links -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
    <!-- header/footer styles -->
    <link rel="stylesheet" href="../styles/style.css" />
    <!-- stylesheet -->
    <link rel="stylesheet" href="../styles/connectionStyle.css" />
   
  </head>

  <body>
    <!-- header section -->
    <%- include("../partials/nav.ejs") %>
    <!-- main content -->
    <main class="container content-container">
      <div class="jumbotron">
        <h2>
          <img id="eventPic" alt="" src="<%= connection.pic %>" /> &nbsp; <span id="eventTitle"><%= connection.title %></span>
          
        </h2>
        <br />
        <br />
        <form method="POST" action="/connection/<%=connection.id%>?_method=PUT">
          <div class="mb-3 form-group">
            <label for="topic" class="form-label">Topic</label>
            <input
              type="text"
              class="form-control"
              id="topic"
              placeholder="Enter the topic of your connection"
              value="<%= connection.category%>"
              name="category"
            />
          </div>
          <div class="mb-3 form-group">
            <label for="title" class="form-label">Title</label>
            <input
              type="text"
              class="form-control"
              id="title"
              placeholder="Enter the title of your connection"
              value="<%= connection.title%>"
              name="title"
            />
          </div>
          <div class="mb-3 form-group">
              <label for="host" class="form-label">Host</label>
              <input
                type="text"
                class="form-control"
                id="host"
                placeholder="Enter the host of your connection"
                value="<%= connection.host%>"
                name="host"
              />
            </div>
            <div class="mb-3 form-group">
              <label for="details" class="form-label">Details</label>
              <textarea              
                class="form-control"
                id="details"
                placeholder="Enter the details of your connection"
                name="description"
              >
                <%= connection.description%>
            </textarea>
            </div>
            <div class="mb-3 form-group">
              <label for="location" class="form-label">Where</label>
              <input
                type="text"
                class="form-control"
                id="location"
                placeholder="Enter the location of your connection"
                value="<%= connection.location%>"
                name="location"
              />
            </div>
            <div class="mb-3 form-group">
              <label for="date" class="form-label">When</label>
              <input
                type="date"
                class="form-control"
                id="date"
                value="<%= connection.date%>"
                name="date"
              />
            </div>
            <div class="mb-3 form-group">
              <label for="start-time" class="form-label">Start</label>
              <input
                type="time"
                class="form-control"
                id="start-time"
                value="<%= connection.start%>"
                name="start"
              />
            </div>
            <div class="mb-3 form-group">
              <label for="end-time" class="form-label">End</label>
              <input
                type="time"
                class="form-control"
                id="end-time"
                value="<%= connection.end%>"
                name="end"
              />
            </div>
            <div class="mb-3 form-group">
              <label for="file" class="form-label">File</label>
              <input
                type="text"
                class="form-control"
                id="file"
                accept="image/*"
                placeholder="Enter the image url"
                value="<%= connection.pic%>"
                name="pic"
              />
            </div>
  
            <div class="d-grid gap-2 submit-btn-container">
              <button type="submit" class="btn btn-primary">Create connection</button>
            </div>
         
        </form>
      </div>
    </main>
    <br>
    <br>
    <!-- footer section -->
    <%- include("../partials/footer.ejs") %>
  </body>
</html>
